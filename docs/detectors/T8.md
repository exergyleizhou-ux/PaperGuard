# T8 — DetectGPT-style Perturbation

> Measures whether the LM rates the manuscript text as more natural than its own paraphrases (a likelihood-curvature signal). Strongly indicative of LLM authorship without needing token logprobs. Opt-in; requires an LLM API.

## What it detects

Measures whether the LM rates the manuscript text as more natural than its own paraphrases (a likelihood-curvature signal). Strongly indicative of LLM authorship without needing token logprobs. Opt-in; requires an LLM API.

Cluster: `paper_mill_signature` — findings from detectors in the same cluster
are treated as non-independent during the BH-FDR + severity escalation
in [`combiner.py`](../../src/paperguard/evidence/combiner.py).

## Inputs

This detector requires data shaped as: `manuscript_text`.

See [`detect()` signature](../../src/paperguard/detectors/t8_detectgpt.py) for
exact type expectations.

## Method

See implementation: [`src/paperguard/detectors/t8_detectgpt.py`](../../src/paperguard/detectors/t8_detectgpt.py)

The algorithm and thresholds are documented in the file header docstring;
they intentionally live with the code so that changing one without the
other is impossible.

## Severity ladder

PaperGuard maps internal scores to the standard five-level Severity:

| Level | Meaning |
|---|---|
| PASS | Not applicable / no signal |
| NOTE | Minor curiosity, archived |
| CONCERN | Single-detector p < 0.01 OR moderate signal |
| SUSPICIOUS | Strong signal OR multi-cluster CONCERN+ |
| CRITICAL | Mathematically impossible result OR cross-cluster ≥ 3 CONCERN+ |

This detector's specific cut-points are in the source file.

## Innocent explanations

Every finding from `T8` carries a non-exhaustive list of plausible
non-fraud explanations. Read them. A flagged finding is **never proof of
misconduct** — it is a request for the author / reviewer / editor to
investigate further with full context.

The current innocent_explanations list lives in
[`t8_detectgpt.py`](../../src/paperguard/detectors/t8_detectgpt.py).

## Known false positives / negatives

| Pattern | Likely outcome |
|---|---|
| Very small sample (N < detector minimum) | Detector marks `applicable=False` (no finding) |
| Data with legitimate structural reasons for the pattern | False positive (use innocent_explanations) |
| Data fabricated by an algorithm that mimics this detector's null distribution | False negative (no detector is silver bullet) |

## Academic basis

Mitchell et al. (2023) DetectGPT: Zero-Shot Machine-Generated Text Detection using Probability Curvature, ICML. PaperGuard adapts the score-curvature idea to chat-completion APIs (the original paper assumes access to white-box token probabilities).

## Source

- Implementation: [`src/paperguard/detectors/t8_detectgpt.py`](../../src/paperguard/detectors/t8_detectgpt.py)
- Tests: see `tests/test_detectors/`
- See also: [`docs/fraud_case_studies.md`](../fraud_case_studies.md) for
  which real misconduct cases motivate this detector.

## Calibration & empirical evidence

| | |
|---|---|
| Shipped in | 2.0.16 |
| Default thresholds | `THRESHOLD_NOTE = 0.0`, `THRESHOLD_SUSPICIOUS = -0.5`, `THRESHOLD_CRITICAL = -1.5` |
| Opt-in via | `paperguard scan --detectgpt-check` (sets `PAPERGUARD_DETECTGPT_CHECK=1`) |
| Cost per scan | ~ 7 LM calls per text segment (1 score-original + 3 perturbations + 3 score-perturbed) × ~ 2 segments per paper = ~ 14 calls |
| Empirically measured | **N=20 controlled benchmark** (10 known-human + 10 known-AI text) shipped in 2.1.10 |
| **Result on cliproxy `gpt-5.4-mini`** | **LR+ = 0** (TPR 0 % at SUSPICIOUS tier, FPR 50 %). 9 / 10 AI samples hit SSL EOF; 1 surviving sample scored 0.0. Human samples spread [-6.0, +1.22] = pure noise. **Two failure modes diagnosed**: (1) paraphraser preserves LLM markers; (2) naturalness rater is miscalibrated on technical prose |
| When it will work | GPT-4-class endpoint with calibrated paraphraser; v9 dataset pre-wired for re-analysis |
| Empirical doc | [t8_endpoint_limitation.md](../t8_endpoint_limitation.md) |

## See also

- [Detector index](README.md)
- [Epistemic position](../epistemic_position.md)
- [Combiner: BH-FDR + severity escalation](../../src/paperguard/evidence/combiner.py)
- Related LLM-text detectors: [T6](T6.md) (lexical), [T7](T7.md) (continuation perplexity)
- [LLM-detection guide](../llm_detection_v2.md)
- [T8 endpoint-limitation study](../t8_endpoint_limitation.md)
